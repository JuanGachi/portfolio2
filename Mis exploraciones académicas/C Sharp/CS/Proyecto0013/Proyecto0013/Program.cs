// operador igualdad
int operando1 = 4;
double operando2 = 3;
bool resultado;
resultado = operando1 == operando2;
Console.WriteLine("Es cierto que el operando 1,("+operando1 +")s igual a operando 2("+operando2+")?"+resultado);
resultado = operando1!=operando2;
Console.WriteLine("Es cierto que el operando 1,(" + operando1 + ") no es igual que el operando 2, (" + operando2 + ")?" + resultado);
resultado = operando1 <operando2;
Console.WriteLine("Es cierto que el operando 1,(" + operando1 + ")es menor que el operando 2, (" + operando2 + ")?" + resultado); 
resultado = operando1 >operando2;
Console.WriteLine("Es cierto que el operando 1,(" + operando1 + ")es mayor que el operando 2, (" + operando2 + ")?" + resultado);
resultado = operando1 >=operando2;
Console.WriteLine("Es cierto que el operando 1,(" + operando1 + ")es mayor que el operando 2, (" + operando2 + ")?" + resultado);
resultado = operando1 <=operando2;
Console.WriteLine("Es cierto que el operando 1,(" + operando1 + ")es mayor que el operando 2, (" + operando2 + ")?" + resultado);